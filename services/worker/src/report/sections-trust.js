import { e, fmtSec, scoreCard, section, table } from "./html-helpers.js";

function eeat(model) {
  const site = model.evidence.site;
  const card = (num, name, found, missing, risk, fix, constrained = false) => `<div class="eeat-card ${constrained ? "eeat-constrained" : "eeat-weak"}"><h4>${num}. ${e(name)}</h4><p class="eeat-found"><strong>Found:</strong> ${e(found)}</p><p class="eeat-missing"><strong>Missing:</strong> ${e(missing)}</p><p style="font-size:.8rem;margin-top:8px"><strong>Risk:</strong> ${e(risk)} <span class="p1-tag">Phase 1</span></p><p style="font-size:.8rem"><strong>Fix:</strong> ${e(fix)} <span class="severity-high">High</span></p></div>`;
  return section("e-e-a-t-conversion-trust-readiness", "07", "E-E-A-T Conversion Trust Readiness", `<p style="font-size:.85rem;color:var(--muted);margin-bottom:16px">On-site, crawl-visible trust evidence. <strong>This is an Omnipressence assessment of observable signals, not a Google-issued numeric score.</strong></p><div style="margin-bottom:16px"><span style="font-weight:700">Readiness Rating:</span> <span style="color:${(model.scores.trust ?? 0) >= 70 ? "var(--green)" : (model.scores.trust ?? 0) >= 40 ? "var(--amber)" : "var(--red)"};font-weight:700;font-size:1.1rem">${e(model.bands.trust || "Not Assessed")}</span></div><div class="eeat-grid-2x2">${card("1", "Experience", site.trust.caseStudies ? "Outcome or case-study language is visible." : "Service and process content is visible.", site.trust.caseStudies ? "More specific examples may still be needed." : "No case-study or outcome proof detected.", "Visitors may not see evidence that the work produces results.", "Publish outcome-based case studies.")}${card("2", "Expertise", site.trust.credentials ? "Credential or experience language is visible." : "Topic expertise is claimed in service content.", site.trust.credentials ? "Per-service attribution may still be weak." : "No credentials or certifications detected.", "Credibility is hard to verify.", "Add credentials and service-specific expertise.")}${card("3", "Authoritativeness", site.trust.testimonials ? "Testimonials or review language is visible." : "Business identity and contact signals are visible.", site.trust.testimonials ? "Broader third-party authority may still be light." : "No testimonial proof detected.", "Prospects may not see independent validation.", "Place specific testimonials near conversion actions.")}${card("4", "Trust", `HTTPS, contact: ${site.trust.contact ? "present" : "limited"}, policies: ${site.trust.policies ? "present" : "not detected"}.`, `${site.schemaTypes.length ? "" : "No schema; "}${site.trust.pricing ? "" : "no pricing context; "}${site.trust.policies ? "" : "no policy reassurance."}`, "Visitors may be uncertain about the next step or commitment.", "Add policies, next-step clarity, and entity schema.", true)}</div><h3 style="margin-top:20px">Phase 2 — Secondary Audit: When Ready to Grow Presence</h3><p style="font-size:.9rem;color:var(--slate);margin-bottom:12px">Once the on-site conversion foundation is improved, Phase 2 can assess external authority and presence growth. These items do not alter the Phase 1 score.</p>${table(["Area", "What Phase 2 Would Assess", "Timing"], [["Backlinks & Referring Domains", "External authority, link quality, and competitor link gaps", "After core service and trust pages are live"], ["Third-Party Reviews", "Review coverage and profile consistency", "After a review collection process exists"], ["External Entity Mentions", "Media, podcasts, directories, and expert profiles", "Ongoing"], ["Long-Term Authority", "Sustained content and referring-domain growth", "Six to twelve months after implementation"]].map((r) => r.map(e)))}`);
}

function cms(model) {
  const site = model.evidence.site;
  const platform = site.platform;
  const proprietary = /GoDaddy|Wix|Squarespace|Shopify/i.test(platform);
  const risk = proprietary ? "Medium — proprietary platform constraints may limit implementation" : platform === "Unknown" ? "Uncertain — platform could not be verified" : "Low to Medium — implementation depends on hosting and theme controls";
  const rows = [
    ["Add meta descriptions", "Likely", "CMS or page settings", "—"],
    ["Add canonical URLs", proprietary ? "Uncertain" : "Likely", proprietary ? "May require code injection or platform setting" : "Framework or SEO configuration", "If platform blocks control"],
    ["Add schema markup", proprietary ? "Uncertain" : "Likely", "Custom code or template access", "If custom code is blocked"],
    ["Fix heading hierarchy", "Likely", "Template or page editor", "—"],
    ["Add security headers", "Hosting-dependent", "Server, CDN, or platform settings", "May require hosting change"],
    ["Create dedicated service pages", "Likely", "Page creation and navigation", "If current plan limits pages"],
    ["Add testimonials and FAQ", "Likely", "Content blocks", "—"],
    ["Add inline lead capture", "Likely", "Native form or embedded form", "If forms are restricted"],
    ["Optimize images", "Likely", "Optimized asset upload", "—"],
  ];
  return section("cms-platform-constraints", "08", "CMS / Platform Constraints", `${table(["Detected Platform", "Value"], [["Platform", e(platform)], ["Hosting / delivery", e(site.pages[0]?.responseHeaders?.server || "Not disclosed")], ["Platform Risk", e(risk)]])}<h3>Platform Limitations Affecting Conversion</h3><ul><li><strong>Metadata control:</strong> Verify page-level title, description, and canonical controls.</li><li><strong>Structured data:</strong> Confirm whether JSON-LD can be added globally and per service.</li><li><strong>Heading structure:</strong> Confirm whether semantic heading levels can be selected independently of visual styling.</li><li><strong>Page architecture:</strong> Confirm the platform or plan supports dedicated service, FAQ, case-study, and policy pages.</li></ul><h3>What This Means for Implementation</h3>${table(["Fix", "Likely Feasible", "May Require Admin / Plan", "May Require Migration"], rows.map((r) => r.map(e)))}<p style="font-size:.85rem;color:var(--muted);margin-top:12px"><strong>Note:</strong> Feasibility is based on captured platform signals. Admin access is required to verify exact controls.</p>`);
}

export { eeat, cms };
