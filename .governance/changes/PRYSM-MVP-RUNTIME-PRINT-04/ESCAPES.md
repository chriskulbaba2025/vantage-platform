# Verification Escape

- Class: production-shaped integration acceptance escape.
- Escaped behavior: the dashboard return link was introduced by the authenticated report proxy after renderer generation, so renderer-only print checks did not see it; Chromium PDF contained browser navigation chrome.
- Permanent correction: route contract is now part of `verify:prysm-closure`; exact authenticated browser acceptance checks its computed print visibility and PDF visual review checks all sheets.
- Preserved output: candidate `0ec242c` report PDF under the closure proof folder, plus the final exact-candidate browser/PDF output after repair.
