# Surgical Change Contract

Required surface: retrieval contract/adapter, additive retrieval persistence, bounded graph traversal/context pack/trace, Ask PRYSM composition, generalized and adversarial tests, tranche proof artifacts.

Expected files: `src/evidence/hybrid-retrieval.js`, `src/evidence/retrieval-trace.js`, `src/evidence/ask-prysm-query.js`, memory/PostgreSQL repositories, migration `005_retrieval_documents.sql`, focused tests, proof directory, and project-context handoff record.

Protected surface: report schemas/renderers, scoring, lifecycle state machine, canonical evidence semantics, auth middleware/authorization, production resources, existing migrations.

Structural budget: one additive migration, one retrieval module, one trace/context module, repository methods, Ask composition, focused tests and governance proof. No dependency additions.

Acceptance: tenant/audit/site scoping occurs before search; secrets are rejected; similarity never becomes evidence; conflict/historical/competitor/currentness remain visible; graph depth/results are bounded; unchanged content does not re-embed; deterministic fallback remains available; exact candidate is independently audited.
